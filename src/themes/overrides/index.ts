import type { Theme } from '@mui/material/styles';

// ==============================|| OVERRIDES - MAIN ||============================== //

export default function componentsOverrides(theme: Theme): Record<string, any> {
  return {};
}